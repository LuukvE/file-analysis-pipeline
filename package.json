{
  "name": "file-analysis-pipeline",
  "workspaces": [
    "server",
    "processor",
    "shared"
  ],
  "scripts": {
    "aws": "docker compose up aws --build",
    "server": "docker compose up server --no-deps --build",
    "processor": "docker compose up processor --no-deps --build",
    "engine": "docker compose up engine --no-deps --build",
    "monitor": "docker compose up monitor --no-deps --build",
    "client": "cd client && bun install --frozen-lockfile && bun run dev"
  }
}
